{
  "name": "webapp",
  "type": "module",
  "scripts": {
    "predev": "node scripts/apply-build-patches.mjs",
    "dev": "vite",
    "prebuild": "node scripts/apply-build-patches.mjs",
    "build": "npm run prebuild && vite build",
    "preview": "wrangler pages dev",
    "deploy": "npm run build && wrangler pages deploy dist --project-name spine-annotator"
  },
  "dependencies": {"hono": "^4.12.25"},
  "devDependencies": {"@cloudflare/workers-types": "^4.12.25", "@hono/vite-build": "^1.2.0", "@hono/vite-dev-server": "^0.18.2", "vite": "^6.3.5", "wrangler": "^4.4.0"}
}
